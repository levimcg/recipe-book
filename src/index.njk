---
title: Recipe Book
description: An online home for my personal recipes.
layout: base.njk
pagination:
  data: collections.recipes
  size: 10
  reverse: true
---
<ul role="list" class="recipe-list flow">
  {% for recipe in pagination.items %}
  {% if recipe.data.hidden != true %}
  <li class="flow">
    {% if recipe.data.icon %}<span aria-hidden="true">{{ recipe.data.icon }}</span>&nbsp;{% endif %}
    <a href="{{ recipe.url }}">{{ recipe.data.title }}</a>
    <p>{{ recipe.data.description }}</p>
  </li>
  {% endif %}
  {% endfor %}
</ul>

{% include "partials/pagination.njk" %}